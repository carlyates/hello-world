require 'sinatra'
require 'json'

post '/payload' do
  push = JSON.parse(request.body.read)
  puts "I got some JSON: #{push.inspect}" #its a mad, mad world but it continues on.
  puts "request type is: #{request.env["X-GitHub-Event"]}" #it did not work :-(  let's try this now.
end
